# Server with multiple locations
server {
    listen 8080;
    root /var/www/html;
    index index.html;

    location /api {
        root /var/www/api;
        methods GET POST;
        autoindex on;
    }
    location /static {
        root /var/www/static;
        index static.html;
        autoindex off;
    }
}
